export enum PassType {
  FLY = "fly",
  SKI = "ski",
  FURKA = "furka",
  GRIMSEL = "grimsel",
  SUSTEN = "susten",
  GOESCHENEN = "goeschenen",
  HUT = "hut",
}

export type SelectedPass = PassType | null; 